# Local development defaults (safe to use without production credentials).
# For production, set DATABASE_URL to your managed Postgres instance.

PORT=4000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/phishing_sim_dashboard?schema=public"
JWT_SECRET="change_me_to_any_long_random_string"
JWT_SECRET_EXPIRES="1h"
